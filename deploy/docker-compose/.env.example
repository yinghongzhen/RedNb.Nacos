# Nacos Docker Compose 环境变量配置
# 复制此文件为 .env 并根据需要修改配置
# 使用方式: cp .env.example .env

# ================================
# MySQL 配置
# ================================
MYSQL_ROOT_PASSWORD=root
MYSQL_PASSWORD=nacos
MYSQL_PORT=3306

# ================================
# Nacos 端口配置
# ================================
# 控制台端口 (Nacos 3.0+ 控制台使用独立端口)
NACOS_CONSOLE_PORT=8080
# HTTP API 端口
NACOS_HTTP_PORT=8848
# gRPC 客户端端口 (HTTP端口 + 1000)
NACOS_GRPC_PORT=9848
# gRPC 服务端端口 (HTTP端口 + 1001)
NACOS_GRPC_SERVER_PORT=9849

# ================================
# JVM 配置 (推荐: 2C4G)
# ================================
# 单机模式推荐
JVM_XMS=512m
JVM_XMX=512m
JVM_XMN=256m

# 集群模式推荐 (取消注释使用)
# JVM_XMS=1g
# JVM_XMX=1g
# JVM_XMN=512m

# ================================
# 鉴权配置 (Nacos 3.0+ 必须配置)
# 重要: 生产环境请务必修改以下密钥！
# ================================

# Token 密钥 (Base64 编码，至少 32 字符)
# 生成方式: echo -n "YourSecretKey32CharactersMinimum" | base64
NACOS_AUTH_TOKEN=VGhpc0lzTXlDdXN0b21TZWNyZXRLZXkwMTIzNDU2Nzg5MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTI=

# 服务端身份标识键
NACOS_AUTH_IDENTITY_KEY=serverIdentity

# 服务端身份标识值
NACOS_AUTH_IDENTITY_VALUE=security
